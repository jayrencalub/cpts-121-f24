#ifndef RECURSION_H
#define RECURSION_H

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

// precondition, m and n > 0
int mult_thru_addition(int m, int n);
int recursion_m_t_a(int m, int n);
int factorial_rec(int n);

#endif
